<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="5">

    <Appenders>
        <!-- Console logging (stderr/stdout distinguished if needed) -->
        <Console name="Console" target="SYSTEM_ERR">
            <PatternLayout pattern="[%d{HH:mm:ss}] %-5level %logger - %msg%n" />
        </Console>

        <!-- File logging for debugging native messaging -->
        <File name="FileLog"
              fileName="logs/native-messaging.log"
              append="true">
            <PatternLayout
                    pattern="[%d{yyyy-MM-dd HH:mm:ss}] %-5level %logger - %msg%n" />
        </File>
    </Appenders>

    <Loggers>
        <!-- your package logged at DEBUG -->
        <Logger name="ge.vakho.native_messaging" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLog"/>
        </Logger>

        <!-- root logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLog"/>
        </Root>
    </Loggers>

</Configuration>

