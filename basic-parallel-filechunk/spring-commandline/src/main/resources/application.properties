# WARNING: Spring @Value does NOT automatically expand JVM system properties like ${user.dir}.
# Using placeholders referencing system properties in application.properties may fail in tests or main context.
# Recommended: either provide relative paths from project root, or resolve system properties programmatically via Environment
# app.copybookPath=${user.dir}/src/main/resources/example.cbl
# app.dataPath=${user.dir}/src/main/resources/example.bin


app.copybookPath=src/main/resources/example.cbl
app.dataPath=src/main/resources/example.bin
app.font=cp037
app.chunkSize=10000
