### ReadCellFormulaX

**Category:** Read / Write

| Key Attributes |
|---------------|
| `Cell`, `SaveTo="[outFormula]"` (direct attribute) |

Use `uip rpa activities get-default-xaml` for full XAML.
