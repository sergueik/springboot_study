# Database Activities Playbooks

**Investigation guide:** [investigation_guide.md](./investigation_guide.md) — data correlation rules and evidence-gathering prerequisites for Database Activities investigations

| Issue | Confidence | Description | Playbook |
|-------|:---:|-------------|----------|
| Execute Query Failures | Medium | `Execute Query` faults across nine surfaces: null/out-of-scope `DatabaseConnection`, post-migration provider mismatch (`Keyword not supported`), SQL syntax / unsafe concatenation incl. smart-quotes (`A database error occurred`), query text pasted into the connection-string field, `TimeoutMS` exceeded (`Timeout expired`), `0xE0434352` CLR crash (oversized result set / Oracle ref cursor / stale package), wrong activity for the statement type (Execute Query vs Execute Non Query), DataTable constraint enablement (`Failed to enable constraints` — SELECT */JOIN returns duplicate-key or NULL rows violating the auto-inferred schema), and connection-input misconfiguration (`ProviderName` null / `Only one of the connections can be used` — neither or both connection methods supplied). Includes anti-patterns and cross-branch prevention. | [execute-query-failures.md](./playbooks/execute-query-failures.md) |
| Connect to Database Failures (Excel / Access via OLE DB / ODBC) | Medium | `Connect to Database` faults across four surfaces: malformed connection string (`Invalid connection string` / `ArgumentException`), ACE/Jet provider not registered or 32-vs-64-bit mismatch (`provider is not registered on the local machine`), file lock (`used by another process`), and provider init / wrong `ProviderName` after Windows-Legacy → Windows migration (`type initializer for 'Microsoft.Data.SqlClient.SqlConnection'`). Centered on the Excel-as-a-database pattern. | [connect-to-database-failures.md](./playbooks/connect-to-database-failures.md) |
| Start Transaction Failures | Medium | `Start Transaction` (`DatabaseTransaction`) scope faults across four transaction-specific surfaces: silent success — a failing child neither rolls back nor re-raises so the job looks green on uncommitted data; scope skips its body with no error on the defective package build (v1.5.0); post-migration provider crash / unresolvable `DatabaseConnection` type after Windows-Legacy → Windows; and the scope's output `DatabaseConnection` not propagated to child `ExistingDbConnection` (null/instant-timeout, or writes that committed outside the transaction). Child SQL/timeout faults route to the data-activity playbooks. | [start-transaction-failures.md](./playbooks/start-transaction-failures.md) |
| Execute Non Query Failures | Medium | `Execute Non Query` (INSERT/UPDATE/DELETE/DDL/stored proc) faults across four surfaces: stored-proc output parameter with `Size = 0` over ODBC (`The Size property has an invalid size of 0`), unsafe SQL construction / parameter mapping / wrong type or DataTable-into-column (`A database error occurred`), empty `Sql` (`CommandText property has not been initialized`), and driver/client library not loadable (`Failed to load library (ErrorCode: 126)`). Covers the parameterize + Bulk Insert/Update guidance and the don't-use-for-SELECT rule. | [execute-non-query-failures.md](./playbooks/execute-non-query-failures.md) |
