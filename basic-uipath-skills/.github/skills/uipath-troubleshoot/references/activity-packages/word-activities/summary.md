# Word Activities Playbooks

**Overview:** [overview.md](./overview.md) — `UiPath.Word.Activities` package, execution models, and common failure patterns
**Investigation guide:** [investigation_guide.md](./investigation_guide.md) — data correlation rules and testing prerequisites for Word Activities investigations

| Issue | Confidence | Description | Playbook |
|-------|:---:|-------------|----------|
| Word COM / Office Interop Failures & Word Process Crashes | Medium | Environmental/host-level COM failures common to **all** Word activities (the scope and any COM-driven activity), not just `Add Picture`: (E1) type library / class not registered (`0x8002801D` / `0x80040154`); (E2) 32/64-bit mismatch between Studio/Robot and Office; (E3) Word busy or blocked (`0x8001010A` / `0x80010001`) from orphaned `WINWORD.EXE` or a modal/Protected-View dialog; (E4) `WINWORD.EXE` crashes mid-operation and the activity then throws `RPC_E_WRONG_THREAD` (`0x8001010E`) on its async-completion path (a known `Add Picture` trigger: inserting a very large image, which the activity cannot resize) | [word-com-interop-failures.md](./playbooks/word-com-interop-failures.md) |
| Word Application Scope — Error Opening Document / Word Not Installed | High | COM Interop cannot create the `Word.Application` instance — no desktop Word (web-only Office, Linux/container robot), 32-bit/64-bit Office–robot bitness mismatch, or damaged Office COM registration (`make sure Word application is installed`, `REGDB_E_CLASSNOTREG`) | [word-scope-com-not-installed.md](./playbooks/word-scope-com-not-installed.md) |
| COM Start Fails in Background / Session 0 | Medium | `Word Application Scope` faults on **COM start** (`WordAppHelpers.StartNewApplication` → `ComAppReferenceCountManager.StartOrAttach`) with `UiPath.Word.WordException: "Error opening document, make sure Word application is installed. If already installed, an Office Repair may be required."` on an unattended robot. Primary cause: process published as a Background Process (`runtimeOptions.requiresUserInteraction: false`) runs in Session 0 with no interactive session, so Office COM can't start — Word opens fine interactively but the headless robot fails. Fix: publish foreground / open an interactive session / Use Foreground (NOT Office Repair). Also covers Word-not-installed and broken-registration | [word-com-start-background-session0.md](./playbooks/word-com-start-background-session0.md) |
| Word Application Scope — "The file appears to be corrupted" | Medium | Opening/saving reports corruption — orphaned WINWORD.EXE holding the lock, in-place template overwrite leaving a half-written source, or Protected View / Mark-of-the-Web blocking the write | [word-scope-file-corrupted.md](./playbooks/word-scope-file-corrupted.md) |
| Word Application Scope — Workflow Hangs / Freezes Indefinitely | Medium | WINWORD.EXE is up but unresponsive because Word opened a background modal prompt (password, document-recovery sidebar, Safe Mode, activation, trust-this-file) that blocks COM calls; invisible when the scope runs unattended | [word-scope-hangs-background-prompt.md](./playbooks/word-scope-hangs-background-prompt.md) |
| Word Application Scope — "Cannot create unknown type WordApplicationScope" | High | Load/compile-time failure: the execution host lacks the `UiPath.Word.Activities` dependency or runs a version without the type; common when a process works in Studio but fails on a remote robot with a different/missing package version | [word-scope-cannot-create-unknown-type.md](./playbooks/word-scope-cannot-create-unknown-type.md) |
| Word Application Scope — File Path Verification Errors / Document Not Found | Medium | The document path does not resolve at runtime — opening a file that should be created (`Create if not exists` unset), a relative path resolved against the wrong working directory, a malformed dynamic path, or an unavailable mapped drive / unhydrated cloud placeholder | [word-scope-file-path-not-found.md](./playbooks/word-scope-file-path-not-found.md) |
| Replace Text in Document — "Application is busy" / COM Interop (0x8001010A) | Medium | The activity's COM call is rejected because WINWORD.EXE is busy — open in the background, locked by another session, or stalled on a hidden modal dialog (`RPC_E_SERVERCALL_RETRYLATER`, `RPC_E_CALL_REJECTED`) | [replace-text-com-busy.md](./playbooks/replace-text-com-busy.md) |
| Replace Text in Document — File Lock / "File is Read-Only" on Save | Medium | The scope cannot persist the edit because the file is in use or read-only — `Auto Save` racing another access in a loop, a `Save As`/rename to the same open path, a concurrent job, or the read-only attribute set | [replace-text-file-locked.md](./playbooks/replace-text-file-locked.md) |
| Replace Text in Document — Placeholder Not Replaced (No Error, No Change) | Medium | Silent miss: the placeholder is split across Word XML runs (edited/reformatted in place) so the exact-string search never matches; the activity succeeds but the document is unchanged | [replace-text-silent-no-substitution.md](./playbooks/replace-text-silent-no-substitution.md) |
| Replace Text in Document — Input String Length Limit (256 chars) | High | Classic versions cap `Search`/`Replace` at 256 characters; longer values raise `ArgumentException` or truncate silently. Fix: upgrade the package or substitute via string/regex in code | [replace-text-length-limit.md](./playbooks/replace-text-length-limit.md) |
| Replace Text in Document — TargetInvocationException / Studio Crash on Drop | Medium | Design-time crash when the activity is dropped or the workflow opened — a Studio↔`UiPath.Word.Activities` version mismatch fails to construct the designer (`TargetInvocationException`). Distinct from the runtime "Cannot create unknown type" fault | [replace-text-version-mismatch.md](./playbooks/replace-text-version-mismatch.md) |
| Replace Text in Document — Placeholder Replaced Once, Then Missing in Loop | Medium | Works on the first loop iteration, then the placeholder is gone on later rows — the workflow edits the template in place, so the first replacement consumes the placeholder. Fix: Copy File to a fresh temp template per iteration | [replace-text-loop-template-overwrite.md](./playbooks/replace-text-loop-template-overwrite.md) |
| Replace Text in Document — Headers / Footers / Text Boxes Not Replaced | Medium | No error, but placeholders in headers, footers, or floating text boxes survive while body text is replaced — older `UiPath.Word.Activities` scans only the primary body. Fix: update the package (newer versions cover shapes/headers) | [replace-text-headers-textboxes-skipped.md](./playbooks/replace-text-headers-textboxes-skipped.md) |
| Replace Text in Document — Multi-line Replacement Loses Formatting | Medium | Replacement text collapses to one line / loses formatting because the `Replace` value carries raw `Environment.NewLine` breaks. Fix: keep replacements plain, or use Bookmarks / Form Fields + Set Bookmark Text for styled multi-line content | [replace-text-multiline-formatting.md](./playbooks/replace-text-multiline-formatting.md) |
| Read Text — Activity Outside Its Container | High | Modern Word-pack `Read Text` faults / warns as invalid when dropped outside a `Use Word File` / `Word Application Scope` (it has no file input of its own). Fix: nest it in a container, or use the standalone `System > File > Word Document` `Read Text` that takes a file path | [read-text-missing-container.md](./playbooks/read-text-missing-container.md) |
| Read Text — Standalone System Activity Fails on .doc | Medium | The standalone `System > File > Word Document` `Read Text` is `.docx`-only and errors / returns nothing on legacy binary `.doc`. Fix: read `.doc` through a `Use Word File` (Interop reads both), or convert to `.docx` first | [read-text-doc-format.md](./playbooks/read-text-doc-format.md) |
| Read Text — Protected View Blocks an Externally-Sourced File | Medium | Reading a file from email / internet / external share faults or hangs because Word opens it in Protected View (Mark-of-the-Web). Fix: unblock the file, add the folder to Trusted Locations, or disable Protected View on the host | [read-text-protected-view.md](./playbooks/read-text-protected-view.md) |
| Read Text — "Application is busy" / COM Interop (0x8001010A) | Medium | WINWORD.EXE busy/locked when the read's COM call fires (`RPC_E_SERVERCALL_RETRYLATER`) — same root cause and fix as the Replace Text COM-busy case (Kill Process WINWORD before the scope / clear the lock) | [replace-text-com-busy.md](./playbooks/replace-text-com-busy.md) |
| Export to PDF — "Command Failed" (Output Directory Missing) | Medium | `Export to PDF` / `Save Document as PDF` faults with a generic `Command Failed` because the output folder doesn't exist (the activity won't auto-create it). Fix: add a `Create Folder` before the export | [export-pdf-missing-output-dir.md](./playbooks/export-pdf-missing-output-dir.md) |
| Export to PDF — Malformed Output Path / Missing .pdf | Medium | The output File Path is built from unformatted concatenation — no `.pdf` suffix, missing/doubled separator, or an empty variable segment — so the save fails. Fix: `Path.Combine(folder, name & ".pdf")`, validate the pieces | [export-pdf-output-path-format.md](./playbooks/export-pdf-output-path-format.md) |
| Export to PDF — COM Interop Hang / Crash / COMException | Medium | The export hangs/crashes or throws `COMException` because an orphaned `WINWORD.EXE` or a locked input document blocks the COM call. Fix: Kill Process WINWORD before the scope, ensure the input is free; persistent → Invoke Code C# `ExportAsFixedFormat` fallback | [export-pdf-com-hang.md](./playbooks/export-pdf-com-hang.md) |
| Export to PDF — TargetInvocationException / Outdated Package | Medium | `TargetInvocationException` / syntax bugs from an outdated `UiPath.Word.Activities` against a newer Studio — same design-time version-mismatch signature and fix (Manage Packages → update) as the Replace Text case | [replace-text-version-mismatch.md](./playbooks/replace-text-version-mismatch.md) |
| Save Document as PDF — COM Wrong-Thread (0x8001010E) | Medium | `Save Document as PDF` (`WordExportToPdf`) or another child of `Word Application Scope` faults casting the document to `Microsoft.Office.Interop.Word._Document` (IID `{0002096B-...}`) with `RPC_E_WRONG_THREAD`. Causes: scope attached to an already-open external Word, that external Word closed mid-run, an off-STA / Session-0 / background runtime, or a non-creator thread (Parallel/Pick/Invoke/coded). `Word Application Scope` exposes no isolated-instance control | [word-export-pdf-com-wrong-thread.md](./playbooks/word-export-pdf-com-wrong-thread.md) |
| Open SharePoint URL — COMException "Command failed" | Medium | `Word Application Scope` / `Use Word File` faults on **open** (`Documents.Open` → `WordDocumentFactory.OpenOrCreateNewDocument`) with `COMException: Command failed`. `FilePath` is a SharePoint/OneDrive web-view **sharing link** (`:w:`/`:f:` segment, `?e=` token) passed verbatim to local Word, which cannot open it. Also covers `CreateNewFile` defaulting `True` against a URL. Fix: use a direct document URL / WebDAV / synced local path, set `CreateNewFile=False`, or download-then-open | [word-open-sharepoint-url-com-command-failed.md](./playbooks/word-open-sharepoint-url-com-command-failed.md) |
| Append Text — "Activity is valid only inside WordApplicationScope" | High | App-Integration `Append Text` faults/warns as invalid outside a `Word Application Scope` / `Use Word File` (no file input of its own). Fix: nest it in a scope, or use the standalone `Word Document` `Append Text` (takes a file path) | [append-text-missing-container.md](./playbooks/append-text-missing-container.md) |
| Append Text — "Archive file cannot be size zero" | Medium | The target `.docx` is a 0-byte file (a renamed `.txt` or a failed/truncated write) and isn't a valid OpenXML package. Fix: delete it + check `Create if not exists`, or fix the upstream that produced the empty file | [append-text-zero-byte-file.md](./playbooks/append-text-zero-byte-file.md) |
| Append Text — "make sure Word application is installed" | High | Same Interop/COM startup failure as Word Application Scope — no desktop Word (server/unattended). Fix: install Word, or use the standalone Word Document activities (no Word needed) | [word-scope-com-not-installed.md](./playbooks/word-scope-com-not-installed.md) |
| Append Text — File Lock / Corrupted File | Medium | The target document is open/locked by a user or an orphaned WINWORD.EXE, blocking write — same lock family as the Word Application Scope corrupted-file case (Kill Process winword before the scope) | [word-scope-file-corrupted.md](./playbooks/word-scope-file-corrupted.md) |
| Append Text — Missing / Unresolved Activity (Package) | Medium | The activity shows missing/invalid (esp. after migration) — same `UiPath.Word.Activities` package-version issue as the Replace Text version-mismatch case (Manage Packages → update / reinstall) | [replace-text-version-mismatch.md](./playbooks/replace-text-version-mismatch.md) |
| Add Picture (WordAddImage) Failures | Medium | `Add Picture` fails to insert an image. Four categories: (C1) activity placed outside a `Use Word File` / `Word Application Scope`; (C2) Word COM interop exception or Word process crash — environmental/host, see [word-com-interop-failures.md](./playbooks/word-com-interop-failures.md); (C3) insertion target (text/bookmark) not found in the open document; (C4) invalid path or an in-memory `UiPath.Core.Image` bound to `Picture to insert` instead of a path string | [add-picture-failures.md](./playbooks/add-picture-failures.md) |
