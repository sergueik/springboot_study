### InvokeVBAArgumentX

**Category:** VBA / Macros

| Key Attributes |
|---------------|
| `ArgumentValue` (nested inside `InvokeVBAX` body) |

Use `uip rpa activities get-default-xaml` for full XAML.
