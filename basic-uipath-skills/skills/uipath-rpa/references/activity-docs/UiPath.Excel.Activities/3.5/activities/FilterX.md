### FilterX

**Category:** Filtering — requires `xmlns:ueabf`

Requires additional namespace:
```xml
xmlns:ueabf="clr-namespace:UiPath.Excel.Activities.Business.Filter;assembly=UiPath.Excel.Activities"
```

| Key Attributes |
|---------------|
| `Range`, `ColumnName`, `HasHeaders`, `ClearFilter`; child `FilterX.FilterArgument` uses `ueabf:FilterArgument` or `ueabf:AdvancedFilterArgument` |

Use `uip rpa activities get-default-xaml` for full XAML.
