# Google Workspace Activities Playbooks

**Investigation guide:** [investigation_guide.md](./investigation_guide.md) — data correlation rules and testing prerequisites for Google Workspace Activities investigations

| Issue | Confidence | Description | Playbook |
|-------|:---:|-------------|----------|
| Gmail — No Matching Email Found (Get Newest Email or trigger debug run) | High | Gmail mailbox contains zero messages matching the configured filter when the activity runs — `Get Newest Email` (`No email matching the search criteria has been found`) or a `NewEmailReceived`/`EmailSent` trigger in debug mode (`No email matching the filter criteria, received/sent in the last 1 hour has been found...`) | [get-newest-email-not-found.md](./playbooks/get-newest-email-not-found.md) |
| Drive — Name Conflict in Specified Folder | High | Drive activity (CreateFolder, CopyFile, MoveFile, Rename, Upload, CreateSpreadsheet, CreateDocument) throws `GSuiteException: Multiple items with the name <name> found in the specified folder.` because the configured `ConflictResolution` cannot resolve a duplicate name | [drive-multiple-items-name-conflict.md](./playbooks/drive-multiple-items-name-conflict.md) |
| Add Sheet — Sheet Name Already Exists | High | Target spreadsheet already contains a sheet (tab) with the configured `SheetName` (case-insensitive) and `ConflictResolution = Fail` (`GSuiteException: A sheet with the same name already exists.`) | [add-sheet-name-conflict.md](./playbooks/add-sheet-name-conflict.md) |
| Resource Not Found / Item Does Not Resolve (404) | High | Activity fails to resolve the configured target because the resource was deleted, trashed, or is not accessible to the connection — a Drive file/folder/spreadsheet/document, a Gmail message by ID (`GetEmailByIdConnections`), or a browser-selected item (`File not found: <id>. [404]` / `HttpStatusCode is NotFound` / `The resource was not found.` / ``Cannot find item configured with connection `<id>` [at path `<path>`].``) | [drive-file-not-found.md](./playbooks/drive-file-not-found.md) |
| Connection, Authentication, and Permission Failures | Medium | Failure in the identity/connection layers, not the resource: auth timeout (`Authentication attempt took longer than <N> seconds...`), expired/invalid token (`Invalid authentication credentials.` / `Authentication error...`), authorization denied (`Permission to the resource was denied.` / `The user does not have sufficient permissions for the file.` / `domainPolicy`), or a raw `ConnectionHttpException` (incl. design-time browse `CNS1044`/`CNS1045`) | [connection-and-auth-failures.md](./playbooks/connection-and-auth-failures.md) |
| Invalid or Null Activity Input (client-side) | Medium | Activity throws before any API call because an input is null, empty, out of range, or an invalid property combination (`Value cannot be null. (Parameter '<x>')` / `The object used in the activity does not exist.` / `Column not found: <name>` / `RelativeRowIndex/RelativeColumnIndex <i> outside the range...` / `Named ranges with cell value are invalid...` / `<property> must be a positive number` / `You do not have the following labels`) | [invalid-or-null-input.md](./playbooks/invalid-or-null-input.md) |
| Transient Service Errors, Rate Limits, and Per-Request Timeouts | Medium | Environmental/temporary failure reaching Google: 5xx (`Internal server error...` / `The service is currently unavailable...`), rate limit/429 (`The daily/user rate limit was exceeded.`), network (`HttpRequestException`), per-request `RequestTimeout` (`A task was canceled.` — not a `TimeoutException`), or the generic fallback `An error occurred in the activity.` | [transient-and-timeout-errors.md](./playbooks/transient-and-timeout-errors.md) |
| Upload — Drive Storage Quota or Shared Drive File Limit Exceeded | High | Upload activity (Drive `UploadFilesConnections`, legacy `UploadFile`, or Gmail send/reply/forward with attachments) fails because the user's Drive storage is full (`storageQuotaExceeded`) or the destination shared drive has hit the 500K item limit (`Upload failed after <N> bytes. ...`) | [upload-storage-quota-exceeded.md](./playbooks/upload-storage-quota-exceeded.md) |
| Sheets — 10 Million Cell Limit Exceeded | High | Sheets write/expand activity (Write Range/Cell/Row/Column, AutoFill, CopyPaste, Add Sheet, plus legacy AppendRow, CopySheet, BatchUpdateValuesScope, Add modes of AddDelete*) fails because the operation would push the spreadsheet past Google's 10M cell cap (`HttpStatusCode is BadRequest. This action would increase the number of cells in the workbook above the limit of 10000000 cells.`) | [sheets-cell-limit-exceeded.md](./playbooks/sheets-cell-limit-exceeded.md) |
| Sheets — Invalid Range / Unable to Parse Range | Medium | Sheets read/write activity passes a malformed A1-notation range to the Sheets API (`HttpStatusCode is BadRequest. Invalid data[0]: Unable to parse range: <SheetName>!<CellRef>`). Common causes: 1-indexing mistake (row 0), empty/null range, unquoted sheet names with spaces, stale sheet references, off-by-one in dynamic construction | [sheets-invalid-range.md](./playbooks/sheets-invalid-range.md) |
