<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Disable all internal logback status messages -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <!-- Silence everything -->
    <root level="OFF"/>

</configuration>
